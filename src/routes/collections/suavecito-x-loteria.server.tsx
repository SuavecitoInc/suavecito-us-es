import {CollectionFeaturedVariants} from '~/components/index.server';

export default function Collection() {
  const handle = 'suavecito-x-loteria';
  return <CollectionFeaturedVariants handle={handle} />;
}
