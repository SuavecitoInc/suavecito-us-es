import {ProductMetafieldPomadeTemplate} from '~/components/index.server';

export default function Product() {
  const handle = 'light-hold-pomade';
  return <ProductMetafieldPomadeTemplate handle={handle} />;
}
